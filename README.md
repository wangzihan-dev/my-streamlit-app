# AI 旅行规划助手

## 运行环境
- Python 3.10+
- Streamlit

## 安装依赖
pip install streamlit requests python-dotenv

## 配置 API
在项目根目录创建 `.env` 文件：
DEEPSEEK_API_KEY=sk-your-key

## 运行
streamlit run app.py